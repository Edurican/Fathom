# =============================================================================
# 🏷️ GitHub Labels Sync
# =============================================================================
# labels.yml 파일을 기반으로 GitHub Labels를 자동 동기화합니다.
# 수동 실행: Actions 탭 → Sync Labels → Run workflow
# =============================================================================

name: Sync Labels

on:
  # labels.yml 변경 시 자동 실행
  push:
    branches: [main]
    paths:
      - '.github/labels.yml'
  # 수동 실행 가능
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Sync labels
        uses: EndBug/label-sync@v2
        with:
          config-file: .github/labels.yml
          # 기존 라벨 중 labels.yml에 없는 것은 삭제하지 않음
          delete-other-labels: false
          token: ${{ secrets.GITHUB_TOKEN }}
