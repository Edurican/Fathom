# [스프링 코어 & Web MVC]

> 📅 2025.12.18 | 🧑‍💻 송형욱 | 🏷️ Spring

---

## 🎯 개요

### 이 주제를 선택한 이유

(왜 이 기술/주제에 관심을 가지게 되었는지)

### 목표

이 발표를 통해:

- 
- 
- 

---

## 📖 본문

# Spring Core

Spring Core는 스프링이 관리할 객체를 어떻게 등록하고 관리할 것인가에 집중한다. 의존성 주입과 제어의 역전을 중심으로 애플리케이션 컴포넌트를 관리한다.

> 즉, Spring Core의 정체성은 '객체의 생명주기를 대신 관리해 줄 테니, 개발자는 비즈니스 로직에만 집중하라' 이다.

스프링 컨테이너가 관리하는 객체를 `Bean`이라고 부르고, 이 `Bean`을 등록하는 방법은 크게 두 가지로 나뉘는데, 여기서 `@Component`와 `@Bean`의 차이가 발생한다.

## `@Component`

- **위치** : 클래스 위
- **용도** : 내가 직접 작성한 클래스를 스프링 빈으로 등록할 때 쓴다.
- **원리** : 스프링이 시작될 때 `@ComponentScan`이라는 기능이 켜져 있으면, 이 어노테이션이 붙은 클래스를 찾아 자동으로 객체를 하나 생성해서 컨테이너에 넣는다.
    1. **탐색** : 스프링 부트가 시작되면 지정된 패키지 하위의 모든 `.class` 파일을 읽어들인다.
    2. **검사** : 리플렉션(클래스 이름만 알면 객체를 생성할 수 있는 API)을 통해 클래스 정보를 보며 `@Component`가 붙어있는지 확인한다.
    3. **인스턴스화** : 대상 클래스를 찾으면 스프링은 `new` 키워드 대신 `Class.getDeclaredConstructor().newInstance()`와 같은 리플렉션 메서드를 사용해 객체를 생성
- **파생 어노테이션** : `@Controller`, `@Service`, `@Repository` 는 모두 `@Component`를 받은 것.
- **예시** :
```java
@Component
public class Something{
    public void doSomething() { ... }
}
```

### ❓서비스, 컨트롤러, 레포지토리 계층에 `@Component`로 퉁쳐도 될까?

**1. `@Controller`**
- **기능적 차이** : 웹 요청(URL)을 인식함. 따라서 URL 매핑을 처리해준다.
```java
// @RestController
public class UserController {
}
```
- 서버는 켜지지만, 이 클래스는 스캔 대상에서 제외되거나 웹 요청을 처리받는 클래스로 인식 되지 않아 404에러를 뱉어낸다.

**2. `@Repository`**
- **기능적 차이** : 예외 변환 기능을 수행한다.
    - 데이터베이스마다 에러의 종류가 다른데, `@Repository`가 붙어 있으면 DB에서 발생한 제각각인 예외들을 스프링이 정의한 예외로 자동으로 바꾸어 던진다. 따라서 개발자는 DB를 교체하더라도 예외 처리 코드를 고칠 필요가 없다.
- `userService.signUp()` 메서드 실행 중 SQL 에러가 터지면, JDBC 드라이버의 raw 에러 그대로인 `SQLException`이나 하이버네이트 예외가 터진다.

**3. `@Service`**
- **기능적 차이** : 현재까지는 없으나, `@Transactional` 어노테이션을 붙여 트랜잭션을 관리

## `@Bean`

- **위치** : 메소드 위
- **용도** : 본인이 직접 작성하지 않은 라이브러리나 설정이 필요한 객체를 빈으로 등록할 때 사용
- **특징** : 외부 라이브러리인 보안 설정 등은 `@Component`를 붙일 수 없으므로 별도의 설정 클래스에서 메소드를 통해 객체를 반환하고 그 위에 `@Bean`을 붙인다.
- **예시** :
```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    private final JwtUtil jwtUtil;
    private final UserRepository userRepository;

    public SecurityConfig(JwtUtil jwtUtil, UserRepository userRepository) {
        this.jwtUtil = jwtUtil;
        this.userRepository = userRepository;
    }

    @Bean // 이 부분
    public PasswordEncoder passwordEncoder() {}
    ... (중략)
    return new BCryptPasswordEncoder();
}
```
`BCryptPasswordEncoder`는 `Spring Security` 라이브러리 안에 있는 `jar` 파일이다. 그런데, 이 클래스는 남이 만든 라이브러리 파일이므로 `@Component`로 빈을 등록할 수 없다. 읽기 전용이기 때문이다. 그래서 설정파일을 만들고, 수동으로 빈을 생성한 뒤 `@Bean`을 붙여준 것이다. 추후에 서비스 계층에서 이 메소드가 필요하다면 이 부분을 꺼내서 주는 것이다.

## `@Configuration`

- **위치** : 클래스 위
- **용도** : 이 클래스는 설정 정보(`Bean` 설정)를 담고 있다는 것을 명시
- **특징**
    - 보통 `@Bean`이 붙은 메소드들은 반드시 `@Configuration`이 붙은 클래스 안에 있어야 한다.
    - 이는 빈을 등록하는 것을 넘어 싱글톤을 보장해주는 중요한 역할을 함. (클래스 안에서 메소드를 여러분 호출해도 실제로는 하나의 객체만 사용되도록)
- **원리**
    1. 스프링은 시작할 때 `CGLIB`라는 라이브러리를 써서 `@Configuration`이 붙은 클래스를 상속받은 가짜 클래스를 만든다.
    2. 그 후 빈을 등록할 때 가짜 클래스를 등록한다(오버라이딩).
       -> 따라서 이미 만든 빈이면 재사용하고 없으면 만든다는 로직을 강제하기 때문에 싱글톤이 깨지지 않는 것이다.

Q. 만약 `@Configuration`을 빼고 `@Bean`만 쓰면?

### `CGLIB`
- **정의** : 클래스의 바이트코드를 조작해서 실행 중에 상속 받은 가짜 클래스를 생성해주는 라이브러리.
- **역할** : 메소드를 호출하면, 바로 객체를 만들지 말고 스프링 컨테이너에 이미 있는지 확인 후 가짜 클래스(싱글톤) 반환.

---

# Spring Web MVC

`Spring Web MVC`는 모든 웹 요청을 `DispatcherServlet`이 받아 적절한 컨트롤러에게 전달하고 결과를 View나 JSON으로 정리해서 응답해 주는 구조를 말한다.

## Spring MVC 요청 동작 원리

> 1. Client가 요청 (Request)
> 2. `DispatcherServlet`이 Request를 처리
> 3. 요청을 분석하여 Request에 대한 Handler Mapping을 한다.
> 4. `Interceptor` : 컨트롤러 진입 전 토큰 등의 검사
> 5. `HandlerAdapter` : 컨트롤러를 실행할 준비를 함 (파라미터 분석 시작)
     >    - `ArgumentResolver` : 파라미터가 필요할 시, URL에서 추출 (`@RequestParam`, `@PathVariable`)
>    - `Message Converter` : JSON이 필요할 시, 자바 객체로 변경 (`@RequestBody`)
> 6. `Controller Method Invoke` : 로직 실행

## 데이터를 받는 어노테이션 (Input)

### `@RequestParam`
- **용도** : URL 뒤에 붙는 `?key=value` 형태의 데이터를 낱개로 받을 때 사용. 게시판 검색이나 필터링 등에 주로 쓰임.
- **작동 방식** : `request.getParameter("key")`와 같이 동작
- **특징** : 기본적으로 필수이고 값이 넘어오지 않으면 400에러가 나지만 옵션으로 끌 수 있다.
- **원리**
    1. 스프링이 URL의 쿼리 스트링 `(?name=song)`을 파싱
    2. `ArgumentResolver`가 어노테이션에 적힌 이름 (`"name"`)을 키로 값을 찾는다.
    3. String으로 받아온 값을 메소드 파라미터 타입에 맞추어 형변환 하여 넣어줌
- **예시** :
```java
@GetMapping("/search")
public String search(
    @RequestParam(value = "keyword") String keyword,
    @RequestParam(value = "page", defaultValue = "1") int page
) {
    return keyword + " 검색, " + page + "페이지";
}
```
```java
GET /search?keyword=마약&page=1
```

### `@PathVariable`
- **용도** : URL 경로 자체를 데이터로 사용할 때 쓰고, 엔터티 고유의 ID를 지정할 때 사용됨.
- **원리**
    1. `@RequestMapping("/users/{id}")`에 정의된 템플릿과 실제 요청 URL을 비교함
    2. 위치를 계산하여 `{id}` 자리에 있는 문자열을 추출
    3. 추출된 값을 파라미터 변수에 할당
- **예시** :
```java
@GetMapping("/users/{id}")
public String getUser(@PathVariable("id") Long userId) {
    return userId + "번 유저 조회";
}
```
```java
GET /users/10
```

### `@ModelAttribute`
- **용도** : 검색 조건이 많거나 HTML `<form>` 태그 전송 시 사용
  `@RequestParam`의 확장판이라고 볼 수 있음. 파라미터가 한 두개면 `@RequestParam`을 쓰지만 검색 조건이 10개라면 객체 하나로 모아서 받는다.
- **작동 방식** : 리플렉션을 사용
- **특징** : **JSON**이 아니다. 따라서 JSON 데이터는 받을 수 없다. 또한 생략이 가능하며, 파라미터에 아무 어노테이션이 없으면 `@ModelAttribute`로 간주됨.
- **원리**
    1. 객체의 기본 생성자를 호출하여 빈 객체를 만든다.
    2. 요청 URL의 파라미터 이름과 똑같은 이름의 프로퍼티를 찾는다.
    3. 값을 넣기 위해 `Setter` 메서드를 실행. (`@ModelAttribute`로 받을 DTO에는 반드시 Setter가 있어야 함)

### `@RequestBody`
- **용도** : 클라이언트가 JSON 형태로 복잡한 데이터를 보낼 때 사용
- **특징** : JSON 문자열을 자바 객체 필드에 넣어줌
- **원리** :
    1. DTO의 기본 생성자를 호출하여 빈 객체 생성 (따라서 DTO 계층에 `@NoArgsConstructor`가 있어야 함)
    2. JSON의 Key와 필드명을 매칭. Setter가 없어도 Reflection을 통해 필드에 직접 값을 주입 가능
- **예시**
```java
{
  "name": "송형욱",
  "age": 26
}
```

## 데이터를 주는 어노테이션 (Output)

### `@ResponseBody`
- **용도** : 서버가 클라이언트에게 결과를 줄 때 사용. 즉, HTML 파일을 찾지 말고 리턴값 자체를 HTTP body에 직접 쓸 때 사용한다.
- **특징** : 객체를 리턴하면 자동으로 JSON으로 바뀌어 나감
```java
@GetMapping("/user")
    @ResponseBody
    public UserDto getUser() {
        return new UserDto("song", 26); // {"name":"song", "age":26}
    }
}
```

### `@RestController`
- `@Controller` + `@ResponseBody`
- **정의** : 웹 페이지를 반환하는 `@Controller`와 달리 데이터 그 자체를 반환하는 어노테이션.
- Java 객체 (Map, List, DTO) 등을 리턴하면 Spring에 내장된 `Jackson`이라는 라이브러리가 자동으로 JSON 형식으로 변환하여 클라이언트에게 전송.
- **흐름**
> 클라이언트가 로그인 요청 -> 디스패처서블릿이 요청을 받아 컨트롤러를 찾음(`UserController`) -> `@RestController`가 요청 본문을 Java 객체 (DTO)로 변환 -> 객체에 아이디, 비밀번호, 토큰을 담은 `ResponseDTO`를 리턴 -> 이 객체를 다시 JSON으로 변환하여 응답

## 헷갈리는 어노테이션의 비교

### `@RequestBody` vs `@ModelAttribute`

**`@RequestBody`** : 클라이언트가 JSON 데이터를 보낼 때 사용
- 클라이언트 요청
```java
POST /api/members
{
    "username": "song",
    "age": 26
}
```
- 백엔드 코드
```java
@PostMapping("/api/members")
public void save(@RequestBody MemberDto member) {
    // Jackson 라이브러리가 JSON을 파싱해서 객체에 넣음
}
```

**`@ModelAttribute`** : 클라이언트가 Form 태그나 쿼리 파라미터를 보낼 때 사용
- 클라이언트 요청 : `GET /members?username=song&age=26`
- 백엔드 코드
```java
@PostMapping("/members/new")
public void save(@ModelAttribute MemberDto member) {
    // Setter를 통해 값을 하나씩 주입함
}
```

### `@PathVariable` vs `@RequestParam`

**값이 없으면 아예 다른 페이지가 되는가?** 이 부분으로 구분하면 쉽다.

- `@PathVariable` : 엔터티 자체를 식별하는 핵심 정보 (`/food/123`)
- `@RequestParam` : 정렬, 필터, 검색처럼 생략해도 되거나 부가적인 정보 기입 (`?type=food`)

```java
    public ApiResult<Boolean> updatePost(
            @PathVariable Long postId,
            @AuthenticationPrincipal UserDetailsImpl userDetails,
            @RequestParam String content,
            @RequestParam Long topicId
    ) {
        Long authenticatedUserId = userDetails.getUser().getId();
        boolean updateOk = this.postService.update(postId, authenticatedUserId, content, topicId);
        return ApiResult.success(updateOk);
    }
```

- `@PathVariable Long postId` : URL에 적힌 이 번호의 게시글을 수정하겠다는 의미
- `@RequestParam String content, @RequestParam Long topicId` : 쿼리 스트링으로 낱개로 데이터를 받아 수정함

예상 결과 : `/api/posts/10?content=수정된내용입니다&topicId=5`

### `@ReqeustParam` vs `ModelAttribute`

이 둘은 들어오는 URL은 같은데, 서버에서 어떻게 받아내느냐의 차이이다.
만일 `GET /members?username=song&age=26` 의 요청이 클라이언트로부터 들어왔다고 가정.

- **`@RequestParam` 사용 시**
```java
@GetMapping("/members")
public String getMember(
    @RequestParam String username, // 1:1 매핑
    @RequestParam int age          // 1:1 매핑
) {
    return "이름:" + username + ", 나이:" + age;
}
```

- **`@ModelAttribute` 사용 시**
```java
@GetMapping("/members")
public String getMember(@ModelAttribute MemberDto memberDto) {
    // memberDto.getUsername(), memberDto.getAge() 안에 이미 값이 들어있음
    return "이름:" + memberDto.getUsername();
}
```

즉, ID나 사용자 이름 하나만 받을 땐 `@RequestParam`을 사용하고, 검색 조건이나 입력 폼은 `@ModelAttribute`를 쓰는 것이 권장 된다.


## 🎬 결론

### 핵심 요약

1. 
2. 
3. 

### 느낀 점

(배운 점, 어려웠던 점, 추가로 공부할 것)

---

## 📚 References

